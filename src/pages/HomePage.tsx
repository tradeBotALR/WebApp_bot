import React from 'react';
import Home from '../features/Home/ui/Home';

/**
 * Главная страница приложения
 * Оборачивает UI-компонент <Home /> — реализация фичи "Главная"
 */
const HomePage: React.FC = () => {
    return <Home />;
};

export default HomePage;